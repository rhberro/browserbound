{
  "name": "browserbond",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "type-check": "pnpm -r run type-check"
  },
  "devDependencies": {
    "@vitest/ui": "^4.1.11",
    "typescript": "^5.3.0",
    "vitest": "^4.1.11"
  }
}
