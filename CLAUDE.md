# BrowserBound — Claude Code Project Configuration

## Agent skills

### Issue tracker

GitHub Issues (this repo's issue board). See `docs/agents/issue-tracker.md`.

### Triage labels

Default triage vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context layout: root `CONTEXT.md` for shared concerns + per-package `CONTEXT.md` for client, server, shared. See `docs/agents/domain.md`.
